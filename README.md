# Chess Clock

FIDE標準の持ち時間に対応した、iPhone向けのチェスクロック。
外部依存ゼロの単一HTML + Service Worker で、**オフラインでも動作**します。

## 時間設定

| プリセット | 内容 |
|---|---|
| クラシカル（FIDE標準） | 90分/40手 + 30分 + 30秒加算 |
| 世界選手権型 | 120分/40手 + 30分 + 30秒加算 |
| クラシカル（単一期間） | 90分 + 30秒加算 |
| ラピッド | 15分 + 10秒加算 |
| ブリッツ | 3分 + 2秒加算 |
| カスタム | 持ち時間・加算・区切り手数・追加時間を自由設定 |

40手を指した瞬間に次の期間の時間が自動で加算され、「第◯期間まであと◯手」を常時表示します。

## 特徴

- **実機同等のデジタル表示** — 表示はすべて7セグメント。1枚の黒い液晶パネルに
  2つのタイマーが左右に並びます。外部フォントを使わずSVGで直接描画しているので、
  どのサイズでも輪郭が潰れず、オフラインでも表示できます

  ```
   1:30  000        30         59:47  012
         00          ◀                  .3
   └時:分┘└手数┘  └加算秒┘
              └秒┘        └手番の向き┘
  ```

- **手番表示** — 中央の三角が動作中の側を指し、その側だけ明るく点灯、
  コロンが1秒周期で点滅します（対局前は先手＝白の側を指します）
- **残り時間の警告** — 残り1分を切るとその側がアンバーに変わり、
  秒の欄が0.1秒単位に切り替わります。時間切れは赤く点滅します
- 設定から先手（白）の側を左右入れ替え可能
- **休憩ポーズ** — 両者の時計を完全停止。休憩の累計時間も記録します
  （※FIDE公式ルールでは対局中に時計を止めることはできません。カジュアル用）
- **スリープ防止** — Wake Lock API で対局中は画面が消えません
- **残り時間の警告** — 1分を切ると背景が赤に、20秒以下で0.1秒単位表示に切り替え
- **設定の保存** — 選んだ持ち時間は次回も維持されます

## 使い方

**最初のタップで対局開始** — チェスは白が先手なので、どちら側をタップしても必ず白の時計から動き出します。

以降は、自分の手を指したら**自分側の画面をタップ**。自分の時計が止まり、相手の時計が動き出します。
手番でない側をタップしても何も起きません（誤操作防止）。

## iPhoneにインストール

1. Safari でこのページを開く
2. 共有ボタン <kbd>􀈂</kbd> → **ホーム画面に追加**
3. ホーム画面のアイコンから起動すると、全画面アプリとして動作します

## 開発

単一ファイル構成です。`index.html` を直接編集してください。

- `index.html` — アプリ本体（HTML + CSS + JS、すべてインライン）
- `sw.js` — オフラインキャッシュ
- `manifest.webmanifest` — PWAマニフェスト
- `make-icon.py` — アイコン生成（`python3 make-icon.py`）

## ライセンス

MIT
